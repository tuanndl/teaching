import React from "react";
import PropTypes from "prop-types";

const MobileHeader = (props) => {
  return <div className="show-mobile">Mobile</div>;
};

MobileHeader.propTypes = {};

export default MobileHeader;
